# Task Concurrency Fixture

This fixture demonstrates task-local concurrency metadata in the task DSL.

## What It Shows

- `describeTask(..., { concurrency: { case: 2 } })`
- cases generated without a per-group override
- a readable example of task-local case caps
