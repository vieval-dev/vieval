# Experiment Metadata Fixture

This fixture demonstrates that `experiment` is run metadata, not a scheduling scope.

## What It Shows

- a normal executable project
- experiment ids passed from the CLI
- an example that keeps experiment tagging separate from task scheduling
