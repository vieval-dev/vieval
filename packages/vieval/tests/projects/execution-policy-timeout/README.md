# Execution Policy Timeout Fixture

This fixture demonstrates a task with one passing case and one timed-out case.

## What It Shows

- `caseOf(..., { timeout })`
- timeout reported as a distinct case outcome
- other cases continue to run and settle normally
