# CLI Concurrency Overrides Fixture

This fixture demonstrates CLI caps applied over project defaults.

## What It Shows

- one runnable project with project/task defaults
- CLI flags such as `--workspace-concurrency`, `--project-concurrency`, and `--task-concurrency`
- an executable example of the current CLI cap surface without introducing unsupported scheduling scopes
